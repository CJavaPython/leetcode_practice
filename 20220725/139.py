from typing import List

# return true : if s can be "segmented" into a space-separated sequence of one of more dictionary words
# same word in the dictionary may be reused multiple times in the segmentation
class Solution:
    def wordBreak(self, s: str, wordDict: List[str]) -> bool:

        return 0
s = Solution()
print(s.wordBreak(s = "leetcode", wordDict = ["leet","code"]))